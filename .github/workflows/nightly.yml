name: Nightly build (develop)

on:
  schedule:
    - cron: "15 2 * * *"   # 02:15 Europe/London
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-develop-nightly:
    name: Build develop (nightly)
    uses: ./.github/workflows/_build.yml
    with:
      lane: develop
      nightly: true
    secrets: inherit
